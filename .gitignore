# Secrets / local config — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# Runtime data / private content — stays on the host
data/
vault/
open-webui-data/
*.db

# OS / editor
.DS_Store
*.swp
*~
